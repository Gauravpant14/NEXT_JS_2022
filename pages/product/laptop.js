

const Laptop = () => {
    return (
        <div>
           <h2>when we give laptop in url (e.g. product/laptop)</h2> 
           <h3>Initially Next will check if the laptop file is availabe inside the folder, if is avaliable then it'll not go into dynamic finind, it will pick that static file</h3>
        </div>
    )
}

export default Laptop
